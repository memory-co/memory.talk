# hooks

| 文件 | 重点 |
|---|---|
| `use-mobile.tsx` | `useIsMobile()`:`< 768px` 断点,shadcn Sidebar 和 `MobileShell` 用 |
| `use-dialog-focus.ts` | `useDialogFocus()`:没有 Trigger 的受控弹窗,关闭时把焦点还给打开前的元素;返回的 props 展开到 `DialogContent` 上 |
